# Subclasses

Will need to work on this later but for the point of subclasses is just to have base commands then have external processes which the subclass will import and use, this won't be noticable to a normal user though and will only be useful for a developer or debugging
